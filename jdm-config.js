// Public configuration only. Never place API keys or private credentials here.
// Set this to the Railway public service URL after the backend is deployed.
window.JACC_JDM_API_BASE_URL = '';
// Set to the same Railway public URL as the bot when direct slip upload is enabled.
window.JACC_PAYMENT_API_BASE_URL = '';
