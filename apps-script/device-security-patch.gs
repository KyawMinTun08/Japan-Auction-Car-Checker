/*
 * DEPRECATED ARCHIVE — JACC's old Flutter-only device patch.
 *
 * The approved Approach B implementation is now in `device-bindings.gs`.
 * It keeps Members A–I unchanged, covers Website/PWA/Flutter, stores hashed
 * bindings in DeviceBindings, stores sessions in AuthSessions, and protects
 * resetMemberDevice with the existing server-key authorization helper.
 *
 * This file intentionally contains no executable declarations so it can remain
 * in the repository as migration history without creating duplicate functions
 * when all .gs files are copied into one Apps Script project.
 */
